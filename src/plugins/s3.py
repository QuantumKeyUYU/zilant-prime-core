"""Плагин-обёртка для работы с S3-подобным хранилищем.

Сейчас в проекте напрямую используется `src/backends/s3_backend.py`,
поэтому данный модуль остаётся пустышкой-«затычкой», чтобы
`plugin_manager` мог корректно обнаружить его при перечислении,
а покрытие тестов не страдало.
"""

# Здесь *специально* нет исполняемого кода → нечего покрывать тестами.
# pragma: no cover
