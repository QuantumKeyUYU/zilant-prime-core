# .semgrep/custom/container-same-path.yml
rules:
- id: container-same-path
  languages: [python]
  severity: WARNING
  message: Source and destination paths must differ
  pattern: pack_file($P, $P, $KEY)
