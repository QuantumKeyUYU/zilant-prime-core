rules:
  - id: click-prompt-insecure
    languages: [python]
    message: "Для скрытия ввода пароля используйте hide_input=True"
    severity: WARNING
    pattern: click.prompt($MSG)
    fix: click.prompt($MSG, hide_input=True)
