rules:
  - id: insecure-hash
    languages: [python]
    message: "MD5 считается небезопасным, используйте SHA256"
    severity: WARNING
    pattern: hashlib.md5($DATA)
    fix: hashlib.sha256($DATA)
