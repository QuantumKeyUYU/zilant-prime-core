# .semgrep/custom/requests-timeout.yml
rules:
- id: requests-timeout
  languages: [python]
  severity: WARNING
  message: "Add a timeout parameter to requests calls"
  pattern: requests.$METHOD($ARGS)
  fix: requests.$METHOD($ARGS, timeout=5)
