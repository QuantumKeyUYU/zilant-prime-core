plugins {
    id 'com.android.application'
    id 'com.chaquo.python' apply false
}
android {
    compileSdkVersion 34
    defaultConfig {
        applicationId 'com.example.zilant'
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName '0.1'
    }
    buildTypes {
        debug {}
        chaquopyDebug {
            initWith debug
            python {
                pip {
                }
            }
        }
    }
}
dependencies {
    implementation project(':zilantjni')
}
