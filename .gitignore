# ---------------------------------------------------------------
# Python-specific ignores
# ---------------------------------------------------------------

# Byte-compiled / optimized / DLL files
# Игнорируем скомпилированные Python-файлы и директории кеша
__pycache__/
*.py[cod]
*$py.class

# C extensions (если есть)
# Игнорируем файлы, сгенерированные для C-расширений
*.so
*.pyd
*.bin

# Distribution / packaging
# Игнорируем файлы, создаваемые при сборке и упаковке дистрибутивов
build/
dist/
*.egg-info/
.eggs/
*.whl

# Installer logs
# Игнорируем логи установщика пакетов (pip)
pip-log.txt
pip-delete-this-directory.txt

# Virtual environments
# Игнорируем папки с виртуальными окружениями
.env/
.venv/
env/
venv/
ENV/

# Environment variables files (commonly used for configuration)
# Игнорируем файлы с переменными окружения, которые обычно содержат чувствительную информацию
.env
.flaskenv

# ---------------------------------------------------------------
# Testing and Static Analysis specific ignores
# ---------------------------------------------------------------

# Unit test / coverage reports
# Игнорируем кеши и отчеты, генерируемые инструментами тестирования и анализа покрытия кода
.pytest_cache/
.cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Hypothesis (for property-based testing)
.hypothesis/

# Ruff cache (linter/formatter cache)
.ruff_cache/

# Mypy (static type checker)
.mypy_cache/
.dmypy.json  # MyPy daemon cache

# ---------------------------------------------------------------
# IDE and OS specific ignores
# ---------------------------------------------------------------

# Editor directories and files
# Игнорируем временные файлы и директории, создаваемые различными IDE и редакторами кода
.idea/             # IntelliJ / PyCharm
.vscode/           # VS Code
*.sublime-project  # Sublime Text
*.sublime-workspace
*.swp              # Vim swap files
*~                 # Backup files (often from Vim or other editors)

# OS files
# Игнорируем файлы, автоматически создаваемые операционными системами
.DS_Store          # macOS
Thumbs.db          # Windows
desktop.ini        # Windows

# ---------------------------------------------------------------
# Project-specific generated files / logs / assets
# ---------------------------------------------------------------

# Logs and databases
# Игнорируем различные файлы логов и базы данных (если они не отслеживаются явно)
*.log
*.sqlite3
db.sqlite3  # Часто используемое имя для SQLite базы данных в проектах

# Pre-commit cache (hooks), but NOT the config file!
# Игнорируем кеш pre-commit хуков, но не сам файл конфигурации (.pre-commit-config.yaml)
.pre-commit/

# Specific project output directories
# Игнорируем сгенерированные файлы или временные директории вашего проекта
docs/_build/
docs/_static/key_lifecycle.png  # Если это сгенерированный файл
ui/dist/

# ---------------------------------------------------------------
# Zilant-specific generated artifacts (reports, logs, keys)
# ---------------------------------------------------------------

# Логи и журналы Zilant
.zilant_log*
docs/.zilant_log*
audit-ledger.jsonl

# Отчёты сканеров / временные артефакты
bandit-report.txt
gype-report.json
sbom.json
security_compliance_report.md
sums*.txt
sum*.txt

# Ключи cosign никогда не должны попадать в репозиторий
.github/cosign.key
.github/cosign.pub

# ---------------------------------------------------------------
# Optional / Other common ignores (uncomment if applicable)
# ---------------------------------------------------------------

# Local settings / configuration files that should not be committed
# local_settings.py
# config.ini.local

# Docker related
# .dockerignore
# Dockerfile.local

# Uploaded media files (if applicable and not stored externally)
# media/
