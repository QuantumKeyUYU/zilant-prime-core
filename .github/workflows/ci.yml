name: CI
on: [push, pull_request]

permissions:
  contents: read      # необходимо checkout'у
  actions: read
  # tests не требуют больше разрешений

jobs:
  build-and-test:
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4        # последняя версия

    - uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: pip

    - name: Install deps
      run: |
        python -m pip install -U pip
        pip install .[dev]

    - name: Run tests
      run: pytest

    # 👉 artefacts
    - name: Upload coverage
      uses: actions/upload-artifact@v4   # ← вместо @v3
      with:
        name: coverage-html
        path: htmlcov/
        if-no-files-found: ignore
